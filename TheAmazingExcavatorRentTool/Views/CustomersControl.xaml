<UserControl x:Class="TheAmazingExcavatorRentTool.Views.CustomersControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TheAmazingExcavatorRentTool.Views"
             xmlns:viewModels="clr-namespace:TheAmazingExcavatorRentTool.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <!-- <UserControl.DataContext> -->
    <!--     <viewModels:CustomerVM/> -->
    <!-- </UserControl.DataContext> -->
  
    <Grid ShowGridLines="False" Background="Cornsilk">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
    
        <DataGrid 
            Name="CustomerGrid" 
            Grid.Row="0" 
            Grid.Column="0" 
            Grid.ColumnSpan="2"
            Grid.RowSpan="3"
            ItemsSource="{Binding Customers}" 
            AutoGenerateColumns="False" 
            CanUserSortColumns="False" 
            AlternatingRowBackground="LightBlue" 
            CanUserReorderColumns="False" 
            SelectionMode="Single" 
            IsReadOnly="True" 
            CanUserResizeColumns="False" 
            SelectionChanged="on_SelectionChanged" 
            GridLinesVisibility="None"
            Style="{StaticResource HeightSetter}"
            >
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nom" Binding="{Binding LastName}" HeaderStyle="{StaticResource CenterGridHeaderStyle}" CellStyle="{StaticResource CenteredGridContent}"/>
                <DataGridTextColumn Header="Prénom" Binding="{Binding FirstName}" HeaderStyle="{StaticResource CenterGridHeaderStyle}" CellStyle="{StaticResource CenteredGridContent}"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" HeaderStyle="{StaticResource CenterGridHeaderStyle}" CellStyle="{StaticResource CenteredGridContent}"/>
                <DataGridTextColumn Header="Date de naissance" Binding="{Binding BirthDate, StringFormat=dd/MM/yyyy}" HeaderStyle="{StaticResource CenterGridHeaderStyle}" CellStyle="{StaticResource CenteredGridContent}"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <GroupBox Grid.Column="2" Header="Modification" BorderThickness="3" BorderBrush="Black" Grid.RowSpan="2" Visibility="Hidden" Name="EditForm">
                <StackPanel Grid.RowSpan="2">
                    
                    <StackPanel Orientation="Horizontal" Style="{StaticResource FirstStackPanelMarginStyling}">
                        <Label Content="Nom:"></Label>
                        <TextBox MaxLength="32" HorizontalAlignment="Left" Name="txtEditLastName" VerticalAlignment="Top"  Style="{StaticResource TextBoxStyling}" Height="25" VerticalContentAlignment="Center" Text="{Binding ElementName=CustomerGrid, Path=SelectedItem.LastName, Mode=OneWay}" TextChanged="ValidateTextFields"/>
                        <Image Source="../Assets/redcross-icon.png" Width="24" Height="24" Margin="8 0 0 0" Name="EditValidLastNameImg"></Image>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Style="{StaticResource StackPanelMarginStyling}">
                        <Label Content="Prénom:"></Label>
                        <TextBox MaxLength="32" HorizontalAlignment="Left" Name="txtEditFirstName" VerticalAlignment="Top" Style="{StaticResource TextBoxStyling}" Height="25" VerticalContentAlignment="Center" Text="{Binding ElementName=CustomerGrid, Path=SelectedItem.FirstName, Mode=OneWay}" TextChanged="ValidateTextFields"/>
                    <Image Source="../Assets/redcross-icon.png" Width="24" Height="24" Margin="8 0 0 0" Name="EditValidFirstNameImg"></Image>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Style="{StaticResource StackPanelMarginStyling}">
                        <Label Content="Email:"></Label>
                        <TextBox MaxLength="64" HorizontalAlignment="Left" Name="txtEditEmail" VerticalAlignment="Top" Style="{StaticResource TextBoxStyling}" Height="25" VerticalContentAlignment="Center" Text="{Binding ElementName=CustomerGrid, Path=SelectedItem.Email, Mode=OneWay}" TextChanged="ValidateTextFields"/>
                        <Image Source="../Assets/redcross-icon.png" Width="24" Height="24" Margin="8 0 0 0" Name="EditValidEmailImg"></Image>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Style="{StaticResource StackPanelMarginStyling}">
                        <Label Content="Dâte de naissance:"></Label>
                        <DatePicker HorizontalAlignment="Left" Name="dpEditBirthDate" VerticalAlignment="Top" Width="300" Height="25" VerticalContentAlignment="Center" SelectedDateChanged="ValidateDatePicker" SelectedDate="{Binding ElementName=CustomerGrid, Path=SelectedItem.BirthDate, Mode=OneWay}">
                            <DatePicker.Resources>
                                <Style TargetType="DatePickerTextBox">
                                    <Setter Property="IsReadOnly" Value="True"/>
                                </Style>
                            </DatePicker.Resources>
                        </DatePicker>                                
                        <Image Source="../Assets/redcross-icon.png" Width="24" Height="24" Margin="8 0 0 0" Name="EditValidBirthDateImg"></Image>
                    </StackPanel>
                    
                    <WrapPanel Margin="0 10 0 0" HorizontalAlignment="Center">
                        <Button Name="editBtn" Content="Enregistrer" Margin="10 0" MinHeight="50" MinWidth="100" Click="ApplyChanges" Style="{StaticResource BtnBorderRadius}"/>
                        <Button Name="clearEditBtn" Content="Effacer" Margin="10 0" MinHeight="50" MinWidth="100" Click="ClearEditForm" Style="{StaticResource BtnBorderRadius}"/>
                    </WrapPanel>
                </StackPanel>
            </GroupBox>
        
        <GroupBox Grid.Column="2" Header="Ajout" BorderThickness="3" BorderBrush="Black" Grid.RowSpan="2" Visibility="Hidden" Name="AddForm">
                <StackPanel Grid.RowSpan="2">
                    
                    <StackPanel Orientation="Horizontal" Style="{StaticResource FirstStackPanelMarginStyling}">
                        <Label Content="Nom:"></Label>
                        <TextBox MaxLength="32" HorizontalAlignment="Left" Name="txtAddLastName" VerticalAlignment="Top"  Style="{StaticResource TextBoxStyling}" Height="25" VerticalContentAlignment="Center" Text="{Binding Path=LastName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextChanged="ValidateTextFields"/>
                        <Image Source="../Assets/redcross-icon.png" Width="24" Height="24" Margin="8 0 0 0" Name="AddValidLastNameImg"></Image>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Style="{StaticResource StackPanelMarginStyling}">
                        <Label Content="Prénom:"></Label>
                        <TextBox MaxLength="32" HorizontalAlignment="Left" Name="txtAddFirstName" VerticalAlignment="Top" Style="{StaticResource TextBoxStyling}" Height="25" VerticalContentAlignment="Center" Text="{Binding Path=FirstName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextChanged="ValidateTextFields"/>
                    <Image Source="../Assets/redcross-icon.png" Width="24" Height="24" Margin="8 0 0 0" Name="AddValidFirstNameImg"></Image>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Style="{StaticResource StackPanelMarginStyling}">
                        <Label Content="Email:"></Label>
                        <TextBox MaxLength="64" HorizontalAlignment="Left" Name="txtAddEmail" VerticalAlignment="Top" Style="{StaticResource TextBoxStyling}" Height="25" VerticalContentAlignment="Center" Text="{Binding Path=Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextChanged="ValidateTextFields"/>
                        <Image Source="../Assets/redcross-icon.png" Width="24" Height="24" Margin="8 0 0 0" Name="AddValidEmailImg"></Image>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Style="{StaticResource StackPanelMarginStyling}">
                        <Label Content="Dâte de naissance:"></Label>
                        <DatePicker HorizontalAlignment="Left" Name="dpAddBirthDate" VerticalAlignment="Top" Width="300" Height="25" VerticalContentAlignment="Center" SelectedDate="{Binding Path=BirthDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedDateChanged="ValidateDatePicker">
                            <DatePicker.Resources>
                                <Style TargetType="DatePickerTextBox">
                                    <Setter Property="IsReadOnly" Value="True"/>
                                </Style>
                            </DatePicker.Resources>
                        </DatePicker>                                
                        <Image Source="../Assets/redcross-icon.png" Width="24" Height="24" Margin="8 0 0 0" Name="AddValidBirthDateImg"></Image>
                    </StackPanel>
                    
                    <WrapPanel Margin="0 10 0 0" HorizontalAlignment="Center">
                        <Button Name="addBtn" Content="Ajouter" Margin="10 0" MinHeight="50" MinWidth="100" Command="{Binding Path=AddCommand}" IsEnabled="False" Style="{StaticResource BtnBorderRadius}"/>
                        <Button Name="clearAddButton" Content="Effacer" Margin="10 0" MinHeight="50" MinWidth="100" Click="ClearAddForm" Style="{StaticResource BtnBorderRadius}"/>
                    </WrapPanel>
                </StackPanel>
            </GroupBox>
    
        <StackPanel Grid.Column="2" Grid.Row="2" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Name="ManageAddFormBtn" Width="150" Height="50" Margin="5" Click="ManageAddForm" Style="{StaticResource BtnBorderRadius}">
                <Image Source="../Assets/add-icon.png" Width="24" Height="24" Name="manageAddBtnImage"></Image>
            </Button>
            <Button Name="ManageEditFormBtn" Width="150" Height="50" Margin="5" IsEnabled="False" Click="ManageEditForm" Style="{StaticResource BtnBorderRadius}">
                <Image Source="../Assets/edit-icon.png" Width="24" Height="24" Name="manageEditBtnImage"></Image>
            </Button>
            <Button Name="DeleteBtn" Width="150" Height="50" Margin="5" IsEnabled="False" Command="{Binding DeleteCommand}" CommandParameter="{Binding ElementName=CustomerGrid, Path=SelectedItem}" Style="{StaticResource BtnBorderRadius}">
                <Image Source="../Assets/redtrashcan-icon.png" Width="24" Height="24" Name="deleteBtnImage"></Image>
            </Button>
        </StackPanel>
            
            
            
            
    </Grid>
</UserControl>
