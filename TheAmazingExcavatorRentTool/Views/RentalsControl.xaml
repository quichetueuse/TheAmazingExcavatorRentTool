<UserControl x:Class="TheAmazingExcavatorRentTool.Views.RentalsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TheAmazingExcavatorRentTool.Views"
             xmlns:viewModels="clr-namespace:TheAmazingExcavatorRentTool.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <!-- <UserControl.DataContext> -->
    <!--     <viewModels:RentalVM/> -->
    <!-- </UserControl.DataContext> -->
    
    <Grid ShowGridLines="False" Background="Cornsilk">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        
        
        <DataGrid 
            Name="RentalGrid" 
            Grid.Row="0" 
            Grid.Column="0" 
            Grid.ColumnSpan="2"
            Grid.RowSpan="3"
            ItemsSource="{Binding Rentals}" 
            AutoGenerateColumns="False" 
            CanUserSortColumns="False" 
            AlternatingRowBackground="LightBlue" 
            CanUserReorderColumns="False" 
            SelectionMode="Single" 
            IsReadOnly="True" 
            CanUserResizeColumns="False" 
            SelectionChanged="on_SelectionChanged" 
            GridLinesVisibility="None"
            Style="{StaticResource HeightSetter}"
        >
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="Client" Binding="{Binding Customer.FullName, StringFormat=Mr {0}}" HeaderStyle="{StaticResource CenterGridHeaderStyle}" CellStyle="{StaticResource CenteredGridContent}"></DataGridTextColumn>
                <DataGridTextColumn Header="Pelleteuse choisi" Binding="{Binding Excavator.Name}" HeaderStyle="{StaticResource CenterGridHeaderStyle}" CellStyle="{StaticResource CenteredGridContent}"/>
                <DataGridTextColumn Header="Début de la location" Binding="{Binding StartDate, StringFormat=dd/MM/yyyy}" HeaderStyle="{StaticResource CenterGridHeaderStyle}" CellStyle="{StaticResource CenteredGridContent}"/>
                <DataGridTextColumn Header="Fin de la location" Binding="{Binding ReturnDate, StringFormat=dd/MM/yyyy}" HeaderStyle="{StaticResource CenterGridHeaderStyle}" CellStyle="{StaticResource CenteredGridContent}"/>
                <DataGridTextColumn Header="Prix de la location" Binding="{Binding Price, StringFormat=€ {0}}" HeaderStyle="{StaticResource CenterGridHeaderStyle}" CellStyle="{StaticResource CenteredGridContent}"/>
            </DataGrid.Columns>
        </DataGrid>
        
        
        <GroupBox Grid.Column="2" Header="Modification" BorderThickness="3" BorderBrush="Black" Grid.RowSpan="2" Visibility="Hidden" Name="EditForm">
            <StackPanel Grid.RowSpan="2">
                
                <StackPanel Orientation="Horizontal" Style="{StaticResource FirstStackPanelMarginStyling}">
                    <Label Content="Nom:"></Label>
                    <ComboBox Name="cbEditCustomer" ItemsSource="{Binding CustomerVm.Customers}" HorizontalAlignment="Left" VerticalAlignment="Top" MinWidth="300" Height="25" VerticalContentAlignment="Center" SelectedItem="{Binding Path=SelectedItem.Customer, ElementName=RentalGrid, Mode=OneWay}" SelectionChanged="ValidateComboboxFields">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock 
                                        Margin="5 0"
                                        VerticalAlignment="Center">
                                        <Run Text="{Binding FullName, Mode=OneWay, StringFormat= Mr {0}}"/>
                                    </TextBlock>
                                    <TextBlock 
                                        Margin="5 0"
                                        Text=" | "
                                       
                                        VerticalAlignment="Center" />
                                    <TextBlock
                                        Margin="5 0"
                                        VerticalAlignment="Center">
                                        <Run Text="{Binding Email, Mode=OneWay}" />
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <Image Source="../Assets/redcross-icon.png" Width="24" Height="24" Margin="8 0 0 0" Name="EditValidCustomerImg"></Image>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" Style="{StaticResource StackPanelMarginStyling}">
                    <Label Content="Prénom:"></Label>
                    <ComboBox Name="cbEditExcavator" ItemsSource="{Binding ExcavatorVm.Excavators}" HorizontalAlignment="Left" VerticalAlignment="Top" MinWidth="300" Height="25" VerticalContentAlignment="Center" SelectedItem="{Binding Path=SelectedItem.Excavator, ElementName=RentalGrid, Mode=OneWay}" SelectionChanged="ValidateComboboxFields">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock 
                                        Margin="5 0"
                                        VerticalAlignment="Center">
                                        <Run Text="{Binding Name, Mode=OneWay}"/>
                                    </TextBlock>
                                    <TextBlock 
                                        Margin="5 0"
                                        Text=" | "
                                       
                                        VerticalAlignment="Center" />
                                    <TextBlock
                                        Margin="5 0"
                                        VerticalAlignment="Center">
                                        <Run Text="{Binding DailyPrice, Mode=OneWay}" />
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <Image Source="../Assets/redcross-icon.png" Width="24" Height="24" Margin="8 0 0 0" Name="EditValidExcavatorImg"></Image>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" Style="{StaticResource StackPanelMarginStyling}">
                    <Label Content="Début de la location:"></Label>
                    <DatePicker HorizontalAlignment="Left" Name="dpEditStartDate" VerticalAlignment="Top" Width="300" Height="25" VerticalContentAlignment="Center" SelectedDateChanged="ValidateDatePicker" SelectedDate="{Binding ElementName=RentalGrid, Path=SelectedItem.StartDate, Mode=OneWay}">
                        <DatePicker.Resources>
                            <Style TargetType="DatePickerTextBox">
                                <Setter Property="IsReadOnly" Value="True"/>
                            </Style>
                        </DatePicker.Resources>
                    </DatePicker>       
                    <Image Source="../Assets/redcross-icon.png" Width="24" Height="24" Margin="8 0 0 0" Name="EditValidStartDateImg"></Image>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" Style="{StaticResource StackPanelMarginStyling}">
                    <Label Content="Fin de la location:"></Label>
                    <DatePicker HorizontalAlignment="Left" Name="dpEditReturnDate" VerticalAlignment="Top" Width="300" Height="25" VerticalContentAlignment="Center" SelectedDateChanged="ValidateDatePicker" SelectedDate="{Binding ElementName=RentalGrid, Path=SelectedItem.ReturnDate, Mode=OneWay}">
                        <DatePicker.Resources>
                            <Style TargetType="DatePickerTextBox">
                                <Setter Property="IsReadOnly" Value="True"/>
                            </Style>
                        </DatePicker.Resources>
                    </DatePicker>                                
                    <Image Source="../Assets/redcross-icon.png" Width="24" Height="24" Margin="8 0 0 0" Name="EditValidReturnDateImg"></Image>
                </StackPanel>
                
                <WrapPanel Margin="0 10 0 0" HorizontalAlignment="Center">
                    <Button Name="editBtn" Content="Enregistrer" Margin="10 0" MinHeight="50" MinWidth="100" Click="ApplyChanges" Style="{StaticResource BtnBorderRadius}"/>
                    <Button Name="clearEditBtn" Content="Effacer" Margin="10 0" MinHeight="50" MinWidth="100" Click="ClearEditForm" Style="{StaticResource BtnBorderRadius}"/>
                </WrapPanel>
            </StackPanel>
        </GroupBox>
        
        <GroupBox Grid.Column="2" Header="Ajout" BorderThickness="3" BorderBrush="Black" Grid.RowSpan="2" Visibility="Hidden" Name="AddForm">
            <StackPanel Grid.RowSpan="2">
                
                <StackPanel Orientation="Horizontal" Style="{StaticResource FirstStackPanelMarginStyling}">
                    <Label Content="Client:"></Label>
                    <ComboBox Name="cbAddCustomer" ItemsSource="{Binding CustomerVm.Customers}" HorizontalAlignment="Left" VerticalAlignment="Top" MinWidth="300" Height="25" VerticalContentAlignment="Center" SelectedItem="{Binding Path=Customer, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectionChanged="ValidateComboboxFields">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock 
                                        Margin="5 0"
                                        VerticalAlignment="Center">
                                        <Run Text="{Binding FullName, Mode=OneWay, StringFormat= Mr {0}}"/>
                                    </TextBlock>
                                    <TextBlock 
                                        Margin="5 0"
                                        Text=" | "
                                       
                                        VerticalAlignment="Center" />
                                    <TextBlock
                                        Margin="5 0"
                                        VerticalAlignment="Center">
                                        <Run Text="{Binding Email, Mode=OneWay}" />
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <Image Source="../Assets/redcross-icon.png" Width="24" Height="24" Margin="8 0 0 0" Name="AddValidCustomerImg"></Image>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" Style="{StaticResource StackPanelMarginStyling}">
                    <Label Content="Pelleteuse:"></Label>
                    <ComboBox Name="cbAddExcavator" ItemsSource="{Binding ExcavatorVm.Excavators}" HorizontalAlignment="Left" VerticalAlignment="Top" MinWidth="300" Height="25" VerticalContentAlignment="Center" SelectedItem="{Binding Path=Excavator, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectionChanged="ValidateComboboxFields">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock 
                                        Margin="5 0"
                                        VerticalAlignment="Center">
                                        <Run Text="{Binding Name, Mode=OneWay}"/>
                                    </TextBlock>
                                    <TextBlock 
                                        Margin="5 0"
                                        Text=" | "
                                       
                                        VerticalAlignment="Center" />
                                    <TextBlock
                                        Margin="5 0"
                                        VerticalAlignment="Center">
                                        <Run Text="{Binding DailyPrice, Mode=OneWay}" />
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                <Image Source="../Assets/redcross-icon.png" Width="24" Height="24" Margin="8 0 0 0" Name="AddValidExcavatorImg"></Image>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" Style="{StaticResource StackPanelMarginStyling}">
                    <Label Content="Début de la location:"></Label>
                    <DatePicker HorizontalAlignment="Left" Name="dpAddStartDate" VerticalAlignment="Top" Width="300" Height="25" VerticalContentAlignment="Center" SelectedDate="{Binding Path=StartDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedDateChanged="ValidateDatePicker">
                        <DatePicker.Resources>
                            <Style TargetType="DatePickerTextBox">
                                <Setter Property="IsReadOnly" Value="True"/>
                            </Style>
                        </DatePicker.Resources>
                    </DatePicker>              
                    <Image Source="../Assets/redcross-icon.png" Width="24" Height="24" Margin="8 0 0 0" Name="AddValidStartDateImg"></Image>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" Style="{StaticResource StackPanelMarginStyling}">
                    <Label Content="Fin de la location:"></Label>
                    <DatePicker HorizontalAlignment="Left" Name="dpAddReturnDate" VerticalAlignment="Top" Width="300" Height="25" VerticalContentAlignment="Center" SelectedDate="{Binding Path=ReturnDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" SelectedDateChanged="ValidateDatePicker">
                        <DatePicker.Resources>
                            <Style TargetType="DatePickerTextBox">
                                <Setter Property="IsReadOnly" Value="True"/>
                            </Style>
                        </DatePicker.Resources>
                    </DatePicker>                                
                    <Image Source="../Assets/redcross-icon.png" Width="24" Height="24" Margin="8 0 0 0" Name="AddValidReturnDateImg"></Image>
                </StackPanel>
                
                <WrapPanel Margin="0 10 0 0" HorizontalAlignment="Center">
                    <Button Name="addBtn" Content="Ajouter" Margin="10 0" MinHeight="50" MinWidth="100" Command="{Binding Path=AddCommand}" IsEnabled="False" Style="{StaticResource BtnBorderRadius}"/>
                    <Button Name="clearAddButton" Content="Effacer" Margin="10 0" MinHeight="50" MinWidth="100" Click="ClearAddForm" Style="{StaticResource BtnBorderRadius}"/>
                </WrapPanel>
            </StackPanel>
        </GroupBox>
        
        <StackPanel Grid.Column="2" Grid.Row="2" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Name="ManageAddFormBtn" Width="150" Height="50" Margin="5" Click="ManageAddForm" Style="{StaticResource BtnBorderRadius}">
                <Image Source="../Assets/add-icon.png" Width="24" Height="24" Name="manageAddBtnImage"></Image>
            </Button>
            <Button Name="ManageEditFormBtn" Width="150" Height="50" Margin="5" IsEnabled="False" Click="ManageEditForm" Style="{StaticResource BtnBorderRadius}">
                <Image Source="../Assets/edit-icon.png" Width="24" Height="24" Name="manageEditBtnImage"></Image>
            </Button>
            <Button Name="DeleteBtn" Width="150" Height="50" Margin="5" IsEnabled="False" Command="{Binding DeleteCommand}" CommandParameter="{Binding ElementName=RentalGrid, Path=SelectedItem}" Style="{StaticResource BtnBorderRadius}">
                <Image Source="../Assets/redtrashcan-icon.png" Width="24" Height="24" Name="deleteBtnImage"></Image>
            </Button>
        </StackPanel>
        
    </Grid>
</UserControl>
