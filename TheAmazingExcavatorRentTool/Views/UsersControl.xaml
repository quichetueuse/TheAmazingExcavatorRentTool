<UserControl x:Class="TheAmazingExcavatorRentTool.Views.UsersControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TheAmazingExcavatorRentTool.Views"
             xmlns:viewModels="clr-namespace:TheAmazingExcavatorRentTool.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             Loaded="UsersControl_OnLoaded">
    <!-- <UserControl.DataContext> -->
    <!--     <viewModels:UserVM/> -->
    <!-- </UserControl.DataContext> -->
  
    <Grid ShowGridLines="False" Background="Cornsilk">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        
        
        <DataGrid 
            Name="UserGrid" 
            Grid.Row="0" 
            Grid.Column="0" 
            Grid.ColumnSpan="2"
            Grid.RowSpan="3"
            ItemsSource="{Binding Users}" 
            AutoGenerateColumns="False" 
            CanUserSortColumns="False" 
            AlternatingRowBackground="LightBlue" 
            CanUserReorderColumns="False" 
            SelectionMode="Single" 
            IsReadOnly="True" 
            CanUserResizeColumns="False" 
            SelectionChanged="on_SelectionChanged" 
            GridLinesVisibility="None"
            Style="{StaticResource HeightSetter}"
        >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nom d'utilisateur" Binding="{Binding Username}" HeaderStyle="{StaticResource CenterGridHeaderStyle}" CellStyle="{StaticResource CenteredGridContent}"/>
                <DataGridCheckBoxColumn Header="Administrateur"  HeaderStyle="{StaticResource CenterGridHeaderStyle}" Binding="{Binding IsAdmin}" CellStyle="{StaticResource CenteredGridContent}"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <GroupBox Grid.Column="2" Header="Modification" BorderThickness="3" BorderBrush="Black" Grid.RowSpan="2" Visibility="Hidden" Name="EditForm">
            <StackPanel Grid.RowSpan="2">
                
                <StackPanel Orientation="Horizontal" Style="{StaticResource FirstStackPanelMarginStyling}">
                    <Label Content="Nom d'utilisateur:"></Label>
                    <TextBox MaxLength="32" HorizontalAlignment="Left" Name="txtEditUsername" VerticalAlignment="Top"  Style="{StaticResource TextBoxStyling}" Height="25" VerticalContentAlignment="Center" Text="{Binding ElementName=UserGrid, Path=SelectedItem.Username, Mode=OneWay}" TextChanged="ValidateTextFields"/>
                    <Image Source="../Assets/checkmark-icon2.png" Width="24" Height="24" Margin="8 0 0 0" Name="EditValidUsernameImg"></Image>
                </StackPanel>
                
                <CheckBox Name="IsEditPassword" Content="Modifier le mot de passe" Checked="IsEditPassword_OnIsEnabledChanged" Unchecked="IsEditPassword_OnIsEnabledChanged"></CheckBox>
                <StackPanel Visibility="Collapsed" Orientation="Horizontal" Style="{StaticResource StackPanelMarginStyling}" Name="PasswordStackPanel">
                    <Label Content="Mot de passe:"></Label>
                    <TextBox MaxLength="128" HorizontalAlignment="Left" Name="txtEditPassword" VerticalAlignment="Top" Style="{StaticResource TextBoxStyling}" Height="25" VerticalContentAlignment="Center" TextChanged="ValidateTextFields"/>
                    <Button MinWidth="75" Content="Générer un mot de passe" Name="editCreatePasswordBtn" Click="CreatePasswordBtn_OnClick"></Button>
                    <Image Source="../Assets/checkmark-icon2.png" Width="24" Height="24" Margin="8 0 0 0" Name="EditValidPasswordImg"></Image>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Style="{StaticResource StackPanelMarginStyling}">
                    <CheckBox Content="Permissions administrateur" Name="checkBEditIsAdmin" VerticalContentAlignment="Center" IsChecked="{Binding SelectedItem.IsAdmin, ElementName=UserGrid, Mode=OneWay}"></CheckBox>
                </StackPanel>
                
                
                <WrapPanel Margin="0 10 0 0" HorizontalAlignment="Center">
                <Button Name="editBtn" Content="Enregistrer" Margin="10 0" MinHeight="50" MinWidth="100" Click="ApplyChanges" Style="{StaticResource BtnBorderRadius}"></Button>
                <Button Name="clearEditBtn" Content="Effacer" Margin="10 0" MinHeight="50" MinWidth="100" Click="ClearEditForm" Style="{StaticResource BtnBorderRadius}"></Button>
                </WrapPanel>
            </StackPanel>
        </GroupBox>
        
        <GroupBox Grid.Column="2" Header="Ajout" BorderThickness="3" BorderBrush="Black" Grid.RowSpan="2" Visibility="Hidden" Name="AddForm">
            <StackPanel Grid.RowSpan="2">
                
                <StackPanel Orientation="Horizontal" Style="{StaticResource FirstStackPanelMarginStyling}">
                    <Label Content="Nom d'utilisateur:"></Label>
                    <TextBox MaxLength="32" HorizontalAlignment="Left" Name="txtAddUsername" VerticalAlignment="Top" Style="{StaticResource TextBoxStyling}" Height="25" VerticalContentAlignment="Center" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextChanged="ValidateTextFields"/>
                    <Image Source="../Assets/redcross-icon.png" Width="24" Height="24" Margin="8 0 0 0" Name="AddValidUsernameImg"></Image>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" Style="{StaticResource StackPanelMarginStyling}">
                    <Label Content="Mot de passe:"></Label>
                    <TextBox MaxLength="128" HorizontalAlignment="Left" Name="txtAddPassword" VerticalAlignment="Top" Style="{StaticResource TextBoxStyling}" Height="25" VerticalContentAlignment="Center" Text="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextChanged="ValidateTextFields"/>
                    <Button MinWidth="75" Content="Générer un mot de passe" Name="addCreatePasswordBtn" Click="CreatePasswordBtn_OnClick"></Button>
                    <Image Source="../Assets/redcross-icon.png" Width="24" Height="24" Margin="8 0 0 0" Name="AddValidPasswordImg"></Image>
                </StackPanel>
                
                
                <StackPanel Orientation="Horizontal" Style="{StaticResource StackPanelMarginStyling}">
                    <CheckBox Content="Permissions administrateur" Name="checkBAddIsAdmin" VerticalContentAlignment="Center" IsChecked="{Binding IsAdmin, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></CheckBox>
                </StackPanel>
                
                <WrapPanel Margin="0 10 0 0" HorizontalAlignment="Center">
                <Button Name="addBtn" Content="Ajouter" Margin="10 0" MinHeight="50" MinWidth="100" Command="{Binding Path=AddCommand}" IsEnabled="False" Style="{StaticResource BtnBorderRadius}"></Button>
                <Button Name="clearAddButton" Content="Effacer" Margin="10 0" MinHeight="50" MinWidth="100" Click="ClearAddForm" Style="{StaticResource BtnBorderRadius}"></Button>
                </WrapPanel>
            </StackPanel>
        </GroupBox>
        
        <StackPanel Grid.Column="2" Grid.Row="2" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Name="ManageAddFormBtn" Width="150" Height="50" Margin="5" Click="ManageAddForm" Style="{StaticResource BtnBorderRadius}">
                <Image Source="../Assets/add-icon.png" Width="24" Height="24" Name="manageAddBtnImage"></Image>
            </Button>
            <Button Name="ManageEditFormBtn" Width="150" Height="50" Margin="5" IsEnabled="False" Click="ManageEditForm" Style="{StaticResource BtnBorderRadius}">
                <Image Source="../Assets/edit-icon.png" Width="24" Height="24" Name="manageEditBtnImage"></Image>
            </Button>
            <Button Name="DeleteBtn" Width="150" Height="50" Margin="5" IsEnabled="False" Command="{Binding DeleteCommand}" CommandParameter="{Binding ElementName=UserGrid, Path=SelectedItem}" Style="{StaticResource BtnBorderRadius}">
                <Image Source="../Assets/redtrashcan-icon.png" Width="24" Height="24" Name="deleteBtnImage"></Image>
            </Button>
        </StackPanel>

    </Grid>

</UserControl>
